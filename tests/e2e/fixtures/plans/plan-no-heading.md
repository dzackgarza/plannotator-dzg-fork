This plan deliberately starts without a Markdown heading. It exists to verify
that the slug derivation falls back gracefully when no `# Heading` is present
on the first line.

The body is several paragraphs of normal prose so the file size is realistic.
The slug should still be deterministic for a given content + project + date so
that a second identical submission does not produce a duplicate history file.

- Bullet one
- Bullet two
- Bullet three
